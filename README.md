# SpringBootInvestmentApp

A Spring Boot application for managing investment assets with CRUD operations, layered architecture, DTO mapping, validation, and an H2 database.

## How to run
- Clone the repo
- Open in your IDE
- Run the Spring Boot application
- Access API at http://localhost:8080/api/assets
